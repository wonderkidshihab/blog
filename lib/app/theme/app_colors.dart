import 'package:flutter/material.dart';

class AppColors {
  static const Color secondaryColor = Color.fromARGB(255, 176, 40, 255);

  static const Color primaryColor = Color.fromARGB(255, 125, 0, 197);

  /// This is the ligher color (White)
  static const Color light = Color(0xFFFFFFFF);
  /// This is the darker color (Black)
  static const Color dark = Color(0xFF000000);
  /// This is the lighter color (Grey)
  static const Color lightGrey = Color(0xFFD8DADC);

  static const Color error = Color(0xFFE74C3C);

  static const success = Color(0xFF2ECC71);
}
